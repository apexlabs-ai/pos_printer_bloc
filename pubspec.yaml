name: pos_printer_bloc
description: A Flutter BLoC component to uniformly handle printing of POS receipts.
version: 0.1.1
homepage: https://github.com/apexlabs-ai/pos_printer_bloc

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: ^4.0.0
  equatable: ^1.2.0
  shared_preferences: ^0.5.7
  esc_pos_utils: ^0.4.1
  esc_pos_bluetooth: ^0.2.8
  flutter_bluetooth_basic: ^0.1.5
  esc_pos_printer: ^3.2.5
  flutter_star_prnt: ^1.0.4
  validators: ^2.0.1
  image: ^2.1.14

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
